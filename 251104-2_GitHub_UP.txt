### GeminiCLIへの指示書 (GitHubアップロード・12月版)

**【実行タスク】**
ローカルリポジトリ（`D:\OneDrive\40_Puzzle\99_sample一覧`）内の変更（`00_image_gallery.html`の修正と`10_12月データ`フォルダの追加）を、GitHubにプッシュ（アップロード）してください。

**【入力情報】**
1.  **ローカルリポジトリのパス:** `D:\OneDrive\40_Puzzle\99_sample一覧`

**【厳守するルール】**
1.  すべてのコマンドは `shell` ツールで順番に実行してください。
2.  認証（Authentication）はすでに完了しているものとして `git push` を実行してください。

**【実行ステップ】**
1.  `cd "D:\OneDrive\40_Puzzle\99_sample一覧"` を実行して、ローカルリポジトリのフォルダに移動します。
2.  `git add 00_image_gallery.html` を実行して、修正したHTMLファイルを追加します。
3.  `git add 10_12月データ/` を実行して、新しいデータフォルダを追加します。
4.  `git commit -m "Add 10_12月データ and update gallery HTML"` を実行して、変更をコミット（記録）します。
5.  `git push` を実行して、GitHubにアップロードします。
6.  `git push` が成功したら、「GitHubへのアップロードが完了しました。」と報告してください。

**【タスクの完遂】**
この指示書のタスクを忠実に完遂してください。